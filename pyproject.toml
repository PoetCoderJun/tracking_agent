[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "robot-agent-runtime"
version = "0.1.0"
description = "Generic robot agent runtime with pluggable skills."
readme = "README.md"
requires-python = ">=3.9,<3.10"
dependencies = [
  "Pillow>=10.0,<12.0",
  "ultralytics>=8.2,<9.0",
  "lap>=0.5.12",
  "opencv-python==4.9.0.80",
  "numpy<2",
  "torch==2.2.2",
  "torchvision==0.17.2",
  "openai>=1.0,<2.0",
  "websockets>=12,<16",
]

[project.scripts]
robot-agent = "backend.cli:main"
robot-agent-perception = "backend.perception.cli:main"
robot-agent-tracking-perception = "scripts.run_tracking_perception:main"
robot-agent-tracking-stack = "scripts.run_tracking_stack:main"
robot-agent-tracking-loop = "backend.tracking.loop:main"
robot-agent-tracking-backend = "backend.tracking.service:main"
robot-agent-tracking-viewer-stream = "viewer.stream:main"
robot-agent-tracking-e2e = "backend.tests.run_demo_tracking_e2e:main"

[dependency-groups]
dev = [
  "pytest>=7.0,<9.0",
]

[tool.setuptools.packages.find]
include = ["agent*", "backend*", "viewer*", "skills*", "scripts*"]

[tool.uv]
default-groups = ["dev"]
